<div class="max-w-container-max mx-auto px-margin-mobile lg:px-margin-desktop py-16">
  <h1 class="font-headline-lg text-headline-lg text-on-surface mb-8 text-center">{{ page.title }}</h1>
  <div class="font-body-lg text-on-surface-variant max-w-3xl mx-auto leading-relaxed">
    {{ page.content }}
  </div>
</div>
